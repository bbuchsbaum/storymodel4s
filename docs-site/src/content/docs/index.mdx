---
title: Evidence-first models of stories and recall
description: A human guide to storymodel4s, a Scala library that records what a story states, what its characters merely say, and what a person recalled - keeping the three apart and citing the sentence behind each one.
template: splash
hero:
  tagline: A young man says "I have no arrows." The saying happened; whether he has none is claimed only inside his speech. This library keeps those apart, and measures what a recall does to them.
  actions:
    - text: Understand the method
      link: /method/orientation/
      icon: right-arrow
      variant: primary
    - text: Inspect one success
      link: /method/one-honest-success/
      icon: open-book
---

import BuildReceipt from '../../components/BuildReceipt.astro';
import Evidence from '../../components/Evidence.astro';
import RouteCard from '../../components/RouteCard.astro';
import { sourceLink } from '../../lib/provenance';

<BuildReceipt />

<div class="route-grid">
  <RouteCard
    eyebrow="Primary route"
    title="Understand the method"
    description="See what the representation preserves, where it abstains, and how a recall maps back to evidence."
    href="/method/orientation/"
    accent="teal"
  />
  <RouteCard
    eyebrow="Scala route"
    title="Use the source library"
    description="Run the current source tree, inspect its typed boundaries, and avoid promises the pre-release API does not make."
    href="/scala/getting-started/"
    accent="coral"
  />
  <RouteCard
    eyebrow="Public, separate"
    title="Contribute safely"
    description="Bring a claim, its evidence, and a failure-sensitive check. Learn the public contribution shape without the internal audit trail."
    href="/collaborate/"
    accent="gold"
  />
</div>

<div class="principle-strip">
  <p>A story model should make uncertainty easier to inspect, not easier to hide.</p>
  <small>The project in one sentence</small>
</div>

## What it does, in one sentence of one story

*The War of the Ghosts* contains the line:

> I have no arrows.

A young man says it. From that single sentence the fixture records **two different things**, and
refuses to merge them:

| Record | Context | Predicate | What it asserts |
| --- | --- | --- | --- |
| `ym1-says-no-arrows` | `world` | `say` | one young man **says** he has no arrows |
| `lack-arrows` | `speechYm1a` | `have` | he has no arrows |

The *saying* happened in the narrated world. Whether he actually **has** no arrows is asserted only
inside his speech, so it lives in the speech context and not in the world. Both records cite
sentence 13, and you can follow either one back to it.

That distinction is the library. Someone recalling this story may report "he had no arrows" as a
plain fact — and that is a real, measurable recall phenomenon, not a parsing error to be corrected
away. A flat list of events cannot tell the two apart, so it cannot notice when a recall promotes
one into the other.

<Evidence
  status="Confirmed"
  source="WarOfTheGhostsModel.scala — both records at span 13, contexts world and speechYm1a"
  href={sourceLink('fixtures/src/main/scala/storymodel4s/fixtures/wog/WarOfTheGhostsModel.scala')}
 />

## What exists now

storymodel4s is a Scala 3 source library for representing written stories, recalls of those
stories, and partial mappings between the two. Its central move is to keep surface evidence,
narrative structure, recall structure, and uncertainty separate enough that a reader can ask how
each conclusion was made.

<Evidence
  status="Confirmed"
  source="README.md and the current module graph"
  href={sourceLink('README.md')}
 />

The current repository includes typed narrative and recall models, alignment machinery, a
researcher-reviewed acceptance fixture, deterministic lexical embedding baselines, and JVM-only
structural and local ONNX adapters. Embeddings remain feature views: they can retrieve or compare;
they do not become narrative claims.

## What does not exist yet

There is no released library artifact, and APIs may change without notice. A raw story does not yet
flow unattended into a complete accepted model. The main story fixture is hand-authored, and a
local encoder's existence is not evidence that it improves recall alignment.

<Evidence
  status="Intent"
  source="README.md maturity boundary and ADR 0005"
  href={sourceLink('README.md')}
 />

That boundary is not fine print. It determines the path through this guide: first understand one
thing the current representation demonstrably preserves, then learn the concepts, and only then
inspect the Scala surface.

## Begin with a difficult distinction

In *The War of the Ghosts*, a wound is reported in speech, the wounded state remains a hypothesis,
and a conflicting bodily state is explicitly negated in the narrated world. A flat event list can
silently promote the report to fact. The fixture is designed to make that mistake visible.

[Inspect the example →](/method/one-honest-success/)
