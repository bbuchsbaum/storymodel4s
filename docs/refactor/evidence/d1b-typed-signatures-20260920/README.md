# D1B typed signature qualification

Status: implementation, the frozen mutation court, real coordinates, all 17 recalls,
and backend byte parity have passed their local courts. Final release qualification
remains pending. D1B is not yet landed. Ordinary Mote updates remain blocked by automatic approval review;
the authorized substantive sequence follows S4c's green local landing.

The candidate separates checked physical `TypedSupport` from optional declared
scoring position, retains point observations and complete result inventories,
and refuses unsupported non-text values at every HSMM v3 wire door. The public
migration is recorded in [API stability](../../../api-stability.md) and
[ADR 0007](../../../adr/0007-film-source-representation.md).

| Court | Current evidence and remaining work |
|---|---|
| Focused implementation | Local `data/study/d1b-20260920/` retains every attempt, including failed construction/test migrations. Focused-8 passed 90 tests at `56b2a51d`; its formatter changed the clone. The [focused/coordinate archive](focused-coordinate-evidence-manifest.json) retains failed attempts and the separately [reconciled capture wrapper failure](capture-compile-2-reconciliation.json), whose sbt compile/tests/format passed with nine tests and one existing conditional skip. |
| Sherlock synthetic view | Explicit attempt 1 retained six passes, two obsolete identity-pin failures, and one conditional neural skip. Attempt 2 passed nine with one skip. The old v1 fingerprint still executes against the declared legacy text view; the typed media v2 identity is separate. |
| Mutations | [67 mutations](mutation-terminal-summary.json) executed at `87848b96`: 67 compiled named kills, 67 passing controls, eight clean main/probe recompiles, and 90 passing restored controls. [The archive manifest](mutation-evidence-manifest.json) binds all 309 receipt/log/XML members. Compile-door cases require actual main and probe recompilation after cleaning both configurations. |
| Real coordinates | The content-free `D1bSherlockSupportCapture` harness binds actual library, view, and retained result support for every row and scene; [the reviewed verifier](support-parity.py) derives independent identity/coordinate expectations and exact ordinal associations. [Execution passed](real-support-manifest.json) at `dcd3f1d3`: all 1,000 rows, 50 scenes and 1,050 retained supports, including unused point row 13, exactly match. [All nine comparator falsifiers](real-support-parity.json) were rejected. |
| All 17 recalls | [The pinned replay recipe](all17-replay.py) and [17-input inventory](all17-inputs.json) are committed; [all 17 replays passed](all17-replay-manifest.json). The exact 2,577-row projection equals frozen SHA-256 `94baf6e4ea94dd9238e377d3434ddc405332af17139b898d3c6a20710c21ce62`. A single participant or the coordinate oracle cannot replace this court. |
| WOG/S0 | Frozen S0 JSON and platform-labelled WOG bytes remain unchanged expectations. [D1B WOG execution passed](hsmm-parity.json) on all three backends at `661c7fca` (4/3/3 tests). S0 full-gate execution remains pending; preexisting Native differences do not establish a new tolerance or identity policy. |
| Provider/consumer | [The first full provider attempt](provider-red-attempt-1-manifest.json) is preserved as red at `850a6d64`: 6,857 tests, 6,854 passed, two skipped and one failed before the last three tasks ran. FilmFestivalSuite still expected an instant-only group to be dropped. The test-only repair requires its exact point, retains world-clock absence and checks a separate interval control. [The supplemental deletion court passed](film-group-regression.json) at `ace77121`, with its named failure/control and three passing restored tests; [earlier unqualified attempts remain archived](film-group-evidence-manifest.json). Replacement full provider, formatting, 13 documentation examples and pinned consumer gates remain pending. |

Adapter guards for impossible malformed metadata beneath sealed, checked intake
values are defensive checks. The reachable adapter witnesses cover differing
valid timebases, composition offsets, exact native and primary point retention,
scene gaps, and receipt inputs. Synthetic inference in the capture harness tests
inventory retention only. It is not a participant replay or a recovery score.

The raw `film-group-2/film-group-regression.json` retains the weaker runner's
historical `qualified: true` flag. The archive manifest supersedes that flag:
attempt 2 is unqualified for closure because its helper/test binding was incomplete.
Only the separately replayed `film-group-3` supplies the supplemental qualification.

Raw annotations, recalls, and generated participant outputs stay ignored and
local. This record makes no scientific, generic film-wire, caption-licensing,
film-compiler, remote CI, or publication claim.
