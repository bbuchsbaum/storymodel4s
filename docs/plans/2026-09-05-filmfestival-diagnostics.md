# Film Festival: locate errors before changing defaults

The owner authorized the five-step evaluation improvement on 2026-09-05. Base:
`257a8db7d1436466aee01b0457fe56fca550ce81`. Work stays in the persistent standalone
clone on `solo/filmfestival-diagnostics-20260905`; the shared planning checkout is untouched.

## Fixed questions and acceptance

1. Join the existing JL/cartoon reports, posterior and voyage records by exact unit identity.
   Report gold-film posterior support, source argmax, final decision and fill origin separately.
   Missing historical nomination ranks/local costs are unavailable, never inferred from mass.
   Compare candidates by content identity across renumbered indices. Fix a deterministic sample
   of corrections/regressions before reading their prose; trace the earliest observed divergence.
2. Add an opt-in, content-free trace of nominations and the actual retained admissible local
   costs. On exactly those states/costs compare independent emission normalization, HSMM
   posterior, scene decode without fill and final decode. Full precision; deterministic ties;
   external states retained. No default change. First prove tracing preserves historical outputs.
   Controlled backward/revisit, ambiguity and external cases must assert behavior, not a score.
3. Inventory source-window and film-cut correspondence before a matched-content comparison.
   Matching candidate counts alone cannot establish matched content. Prepare a deterministic
   matched-window/budget workflow that refuses missing correspondence evidence; aggregation
   choices must use source descriptions alone. Run only comparisons the available evidence supports.
4. Build a blinded local annotation packet and strict answer ingestion for localization,
   ambiguity, merge, revisit and external material. Model choices and old gold stay hidden from
   the annotator. Synthetic controls are engineering evidence, not independent human labels.
   Existing participants remain development data. New human labels/verified clips and a genuinely
   unseen confirmation sample are external inputs, not things an agent can declare into existence.
5. Provide one compact replay command and a bound manifest. Reuse saved outputs before another
   batch. Reject changed inputs/populations, nonfinite values and unsupported schema versions.
   Record all results, runtime, mutation witnesses and the full landing gate.

Primary scoring retains the stabilized first-run gold eligibility and pooled unit-weighted
accuracy, including unanchored units. Paired uncertainty resamples participants (4,000 draws,
seed 20260904); it does not generalize to unseen films. Report participant and film breakdowns.
Stage-local retrieval coverage is an upper bound only for selectors confined to those candidates;
the existing fill policy can widen that set and must be shown separately.

No parameter sweep or source/semantic admission claim is authorized by an improving development
number. The next diagnostic configuration is fixed: the historical settings, with intermediate
stage outputs added. Independent local selection is derived directly from retained costs and
temperature, with no transitions or refinement; it is a comparator, not calibrated confidence.

## Progress

- Protocol recorded before new stage-level outcomes. Independent annotation/verified clip inputs
  requested from the owner while implementation proceeds.
- Stage measurements and workflow outcomes are recorded in
  [the diagnostic results](2026-09-05-filmfestival-diagnostic-results.md). Steps 1, 2 and 5 ran;
  steps 3 and 4 have tested machinery and local worksheets/packets, with independent inputs pending.
